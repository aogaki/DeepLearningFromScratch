# フレームワーク本番化ノート — 事件簿と v1.0 要求台帳

このリポジトリは『ゼロから作る Deep Learning』全巻の写経学習用だが、vol3 で作った
自動微分フレームワークは、将来 **別リポジトリの独立ソフトウェア** として作り直し、
研究(核物理 — ΔE-E 粒子識別の事後確率推定、SBI)の本番ツールへ育てる(README のロードマップ参照)。

この文書はそのための **設計仕様の初稿となる台帳**。3 部構成:

1. **持ち越す設計** — vol1〜vol5 で実証済みの型
2. **事件簿** — 実際に踏んだ失敗と、その一般化(同じ過ちを二度踏まないためのリスト)
3. **v1.0 要求リスト+摩擦台帳** — vol5 後半(ステップ 6〜10、PyTorch 写経区間)で
   見つかる摩擦・欠品・意味論差を随時追記していく生きたリスト

運用: 写経中に摩擦が見つかったら、その場で末尾の台帳に追記する(記録者: Claude)。
新リポジトリ着工時に、この文書がそのまま設計レビューの土台になる。

## 決定事項(2026-07-29)

- 本番フレームワークは **vol5 完走後に別リポジトリで新規構築** する。既存クレートの
  昇格・移設ではなく、vol3 を「自分で書いた仕様書 = golden 参照」としての再実装。
  二周目の再発明は冗長ではなく、写経プロジェクトの創設原理(再実装して理解する)の
  本番スケールへの適用である。
- 書き手は Claude(本番ツール化モード = vibe coding)。品質の錨は
  (1) テストスイート (2) vol3 との golden 一致検証 (3) PyTorch パリティ。
  ユーザーは実装の逐行ではなく **テストと API の形** をレビューする。
- リポジトリ間の取り込みは git submodule ではなく **Cargo の git / path 依存** を使う
  (submodule は pin ずれ・同期の手間で不利。Cargo が依存解決を担う方が Rust 的)。
- vol5 後半は従来通り vol3 への path 依存で写経を続行する。凍結を vol5 完走後まで
  遅らせるのは、生成モデル(VAE・拡散)が要求する API を知る前に v1.0 を固めない ため。

## 決定事項・追補(2026-07-30 — v1.0 スコープ確定)

vol5 卒業ラン(step10 一晩学習)前夜に 4 分岐を決着:

1. **v1.0 の完了定義 = 卒業試験+CPU 並列**。卒業試験 = vol5 の golden 4 本
   (step06 SGD 回帰 / step07 VAE / step09 UNet 拡散 / step10 条件付き+CFG)を
   新スタックでそのまま green にすること。golden npz は**フレームワーク非依存の契約書**
   なので書き直し不要 — 生成スクリプト(vol5/tools/)ごと新リポの tools/ へ持ち込む。
   加えて rayon 並列で UNet iter の実測目標(目標値は M3 で設定。参考実測:
   vol3 単線 6.5 s/iter @batch128、matmul スレッド化のみでは ×1.3 が天井)。
   GUI は v1.1、wgpu は v2 として別フェーズに切る。
2. **単一クレート開始**。モジュール分割は vol3 の家風(variable/functions/layers/…)を
   踏襲し、クレート分割は境界が実証されてから。
3. **API は vol3 継承+台帳の負債返済のみ**(Module/forward 分離、随伴ペア標準形、
   per-param Adam、レイヤ独立シード、log-space、Embedding 一級、演算子レベル rayon)。
   PyTorch 風への全面再設計はしない — golden 移植コストと二周目の学習コストを最小化。
4. **最初から public**。台帳を公開文書とした流儀の延長(README に学習プロジェクト由来を明記)。

未決(着工時に決める): リポジトリ名、rayon の速度目標値、golden npz の置き方
(再生成可能なので gitignore+スクリプト生成を第一候補に、サイズ次第で判断)。

**コンパイル時形状検査(const generics)の見送り(2026-07-30 決定)**: 形状を型に
載せる案は 2 段階ある — ①rank のみ(Burn の `Tensor<B, D>`、安定版で可能)
②全サイズ(dfdx 方式、matmul の内側次元不一致をコンパイルエラー化)。②は
(a) 型内算術(Conv 出力サイズ等)が nightly `generic_const_exprs` 依存で reshape 周りが
型パズル化 (b) **JSON→モデル組立(GUI 構想)と根本衝突**(実行時に読んだサイズは型に
載らない。バッチサイズの実行時変動も同根)のため不採用。v1.0 は動的形状(ArrayD 継承)
+**レジストリの形状推論で「組立時に即死」**(コンパイル時ではないが学習開始前に落ちる)。
①rank のみは安定版で可能な穏当な強化として将来の選択肢に残すが、API 全面波及のため
v1.0 では見送り。Burn も全サイズは型に載せていない — 同じ損益計算。

**参照実装の役割分担(2026-07-30 議論)**: パリティの意味論の正は **PyTorch 一本**に保つ
(正が二つあると、ズレたときにどちらが間違いか判定できなくなる。golden 儀式の
パイプラインも Python/numpy 前提)。Burn・candle は正としてではなく、
①**M0/M1 凍結前の設計調査**(Burn の Module 分離・`Tensor<Backend, D>` の
バックエンド・ジェネリクス・wgpu バックエンドの実物 — 我々の台帳課題の先行解)
②**M3/v2 の性能ヤードスティック**(同じ Rust・同ハードでの公平な速度物差し)として使う。

**デバイス切替の設計条件(2026-07-30 議論)**: CPU スレッド数はただの実行時ノブだが、
**並列リダクションは決定論を規約化**(チャンク分割固定+部分和の結合順固定 — シード固定
テストとパリティ儀式を守るため。matmul スレッド化が決定論を保ったのは k 軸不分割の性質、
同じ性質を rayon 側で意図的に作る)。GPU は `to_device()` 一発の UX を目指すが、
vol1 の教訓により**演算単位のオフロードは罠**(転送で CPU より遅くなる) — 抽象化は
関数レベルでなく**ストレージレベル**(Variable のバッファが ndarray か wgpu::Buffer か)で行い、
転送は境界のみ。v1.0 で払う保険は 3 点だけ: ①数値演算は名前付き関数経由 ②Variable の
データ本体はアクセサの裏(将来の storage enum の席)③f32 統一。ndarray が公開 API に
裸で漏れると v2 は「追加」でなく「書き直し」になる。CPU↔GPU のパリティは統計 tier で
(加算順序が変わるためビット一致は求めない — 既存ドクトリン適用)。

**GUI・マニュアルの設計方針(2026-07-30 議論)**: GUI とエンジンは **JSON のモデル記述
だけで会話**する(ユーザーの直感どおり)。優先はインタプリタ(JSON→実行時にモデル組立、
数日級)で、Rust コード生成は「コードへ卒業する人向け export」に格下げ。GUI 本体は
Tauri+既存 JS ノードエディタ(litegraph.js / rete.js 等)を被せる — ノードエディタは
自作しない(プロトタイプ 1〜2 週間級)。鍵は**演算子メタデータのレジストリを単一の正**
にすること: 各演算が名前・引数・形状規則・説明を自己申告 → JSON 検証・GUI パレット・
リファレンスを全部そこから生成し、演算子追加で三重帳簿が発生しない構造にする。
v1.0 にはレジストリ骨格+形状推論だけ仕込む(config 駆動実験・再現性記録にも効く)。
マニュアルは rustdoc+doctest(CI でテストされるので腐らない)+卒業試験 4 example が
実行可能チュートリアルを兼ねる。GUI の守備範囲は「入口とデモ」— 研究の自作損失は
コードの領分(Keras と同じ線引き)。

**アーキテクチャ境界(2026-07-30 確認)**: 研究解析はフレームワークに**含めない**。
vol5→vol3 と同じ関係で、解析(⁵³Mn PID・Oslo 法など)は**別リポジトリ**が
Cargo git 依存でエンジンを取り込む。理由: ①public 境界(研究データの混入防止)
②依存の向き(エンジンにドメイン語彙を入れない — ROOT と解析コードの関係)
③再現性(解析側 Cargo.lock がエンジンのコミットを固定 = 論文時点の凍結が自動)。
v1.0 リポに入るのはエンジン+ドメイン非依存の example(卒業試験 4 本が使用例を兼ねる)まで。
GUI は汎用部品(曲線モニタ)がフレームワーク側、研究特化画面(PID 操縦席)は解析リポ側。

## 1. 持ち越す設計(実証済みの型)

vol3 で確立し、vol4(DQN/方策勾配)・vol5 で実戦済みのもの。新リポジトリの出発点。

- **Variable は `Rc<RefCell<Inner>>` の薄いハンドル**。Python の「全てが共有参照」の明示化。
  リーク検証は Weak 参照の観測テストで。
- **関数の能力を trait で 3 分割**: `Forward`(順伝播のみ・数値微分用)/
  `Function: Forward`(+backward)/ `Creator`(グラフ遡行の最小界面)。
  `call(self)` が self を消費して `Node<F>` としてグラフへ移る —
  「入力未設定」状態が型で存在しない。
- **世代管理つき backward**(トポロジカル順+勾配累積)、`no_grad`/`test_mode` は
  thread_local + RAII ガード(`#[must_use]`)。
- **grad は Variable**(double backprop)。backward 自体が Variable 演算でグラフを作る。
- **broadcast_to / sum_to の双対**、不変条件 `grad.shape == data.shape` を debug_assert。
- **Layer trait**: `params()`(名前なしホットパス)と `named_params()`("l1/W" 階層名)の
  分離。Optimizer は `params()` の Rc ハンドル clone を保持(split-borrow 問題が消滅)。
- **合成スタイル**: dropout・ReLU は「マスク定数 × Mul」で合成 — 手書き backward なし、
  二階微分も自動で正しい。プリミティブを増やすより合成を優先する。
- **随伴ペアの型**(線形演算子): A と Aᵀ を Function ペアにして**互いの backward** にする
  (Im2Col/Col2Im、Concat/SliceAxis、Upsample/UpsampleGrad)。二階が自動で閉じ、
  silent なグラフ切断や `unimplemented!` が構造的に消える。検証はセットで:
  ①手計算値(非対称データ)②内積の随伴検証(Σ gy⊙(Ax) の数値微分 = Aᵀgy)③二階の配線テスト。
- **DataLoader は `IntoIterator for &mut`** で「1 エポック = 1 for ループ」。
  Dataset は関連型 Target(usize 固定にしない)。
- **save/load は npz・2 パス atomic**(全検証後に一括代入 — 部分書き込みなし)。
  キー "l0/W" 形式は Python np.load がそのまま読める(パリティ橋)。
- **f32 で統一**(将来の wgpu / WGSL に f64 が無い)。数値演算は名前付き関数の裏に
  隠しておく(GPU 差し替えの継ぎ目を最初から作る — vol1 の wgpu 経験の教訓)。
- **RNG は常に引数注入**(`&mut impl Rng`、シード固定テストの前提)。乱数系は
  rand + rand_distr に統一(ndarray-rand は使わない — 事件簿参照)。

## 2. 事件簿 — 同じ過ちを二度踏まないために

### 数値(f32)の罠

| 事件 | 一般化 |
| --- | --- |
| EM の負担率崩壊(vol5): 本の f64 前提の初期値で指数部が −200 → `exp` が f32 で 0(f64 なら 1e-87 で生存)、全成分の密度消失 | 尤度・確率は **対数空間で持つ** のが本番の既定。`log_sum_exp` を標準装備。f32 の閾値: exp の引数 ≈ −87 でアンダーフロー |
| EM の対数尤度がプラトーで −4e-4 微減(vol5): 理論は単調非減少のはずが f32 ノイズ(\|LL\| × 相対精度 1e-7)に埋まる | **定理の assert は誤差付き** で。「厳密な ≥」は f32 では成立しない |
| clippy `excessive_precision` 111 連発(vol4 CartPole golden) | f32 の有効桁は約 7 桁。golden 生成は Python 側で `str(np.float32(v))` により最短表記を吐く |
| 数値微分が f32 で有効 2〜3 桁(vol1)、刻みも f64 用の 1e-4 では破綻 | f32 の数値微分は eps = ∛ε ≈ 5e-3、テスト tol は 1e-2 級。gradient check の限界を知る(下記「検証」も参照) |

### 依存関係の罠

| 事件 | 一般化 |
| --- | --- |
| rand 二重化 ×2 回(vol1: 0.9 と 0.10 が並存 / vol5: ndarray-rand 0.16 が rand 0.9 系を引き込み、宣言した 0.10 が未使用に) | 依存を足したら **`cargo tree` で確認** する習慣。乱数は rand + rand_distr に統一し、ndarray-rand は使わない(`random_array` は 5 行で自作できる) |
| ndarray-linalg のリンクエラー(vol5): undefined `_cblas_*` `_dgetrf_` — BLAS/LAPACK バックエンド未指定。C コンパイラは無実 | **外部数値バックエンド依存を持ち込まない**。必要な線形代数(det・inv・Cholesky)は自作済みで移植可能。GPU も wgpu 自前路線で外部依存ゼロを貫く |

### API・設計の罠

| 事件 | 一般化 |
| --- | --- |
| LSTM の `unchain_backward` が dyn 経由で no-op(vol3): 固有メソッドが trait メソッドを陰り、具象呼びだけ動く | trait のデフォルト実装を差し替えるときは必ず `impl Trait for` 側に書き、**`&dyn` 経由の回帰テスト** で封印 |
| silent full BPTT(vol3): truncated BPTT の切断ピンは 2 本(レイヤの h と累積 loss の Add 連鎖)。後者を切り忘れても動くが二次コスト | 「呼び忘れられる規律」は設計で消す。切断は可能な限り **型で不可能** にする(例: Actor-Critic の切断 2 点を f32 境界の計算で表現 — `unchain()` 呼び出し自体が不要になった) |
| `zip` は黙って短い方に合わせる(vol4 DQN の週次同期で顕在化しかけた) | パラメータ列の zip コピーには **長さ assert** を必ず添える |
| 勾配を「切る/運ぶ」の使い分け(vol4): Q 学習はターゲットを `.data()` で切る、方策勾配は π(a\|s) をグラフ付きで運ぶ — 真逆の習慣が同居 | どちらの向きも「**切れていれば optimizer 不動 / 繋がっていれば動く**」の隔離テストで封印できる。フレームワークはこのテストを書きやすい API(パラメータのスナップショット比較)を提供する |

### テスト・検証の罠

| 事件 | 一般化 |
| --- | --- |
| pooling の転置ずれ(vol3): gradient check 全 green のまま値が間違っていた | **gradient check は自己一致検証にすぎない**。縮約・並べ替え系(reshape/transpose/pooling/im2col)は手計算の値テスト必須、かつ **非対称データで**(全同値データは並べ替えに盲目) |
| 2 マス世界の壁 −1 転記漏れ(vol4 GridWorld)・CartPole 物理の写し間違いリスク | 環境・データの転記ミスは自己一致テストで検出不能。**本家を実走した golden とのパリティ** を儀式化(VGG16 1e-4 一致、CartPole 軌道 1e-4、Old Faithful は文献正典値) |
| DQN 初回実験の判定不能(vol4): 単一シードでは「渋いが正常/壊れてる」を区別できず、容量不足がシード運と紛らわしい | 確率的学習は **シード sweep+対照群**。更新則が異なる比較では同一シードでも経験列が分岐するため **ペアドシード比較は不成立**(改善に見えるのは平均への回帰) |
| 「上手くいきすぎると落ちる」assert(vol4 PG): 上限 assert に自分の実験出力が反証 | 下限 = 学習の証明、上限 = 演出。**主張はギャップ assert に一本化** |
| Double DQN が成績で差なし(vol4) | 二次効果(成績)で差が出ないときは **一次量を直接測る**(Q(s₀) の過大評価で 4σ 検出)。物理の流儀がそのまま通用する |
| 本の「図を目視」文化 | **図は統計量 assert に翻訳**(規格化のリーマン和、Φ による真値較正、標本共分散の非対角込み検証、正典値パリティ) |
| `#[should_panic(expected)]` が別のパニックを拾う | panic テストは **パニックする 1 呼び出しに最小化** |
| 9.1→9.2 変質事件(vol4): リファクタ中に可変キャプチャ付き map が畳み込みを飲み込み、アルゴリズムが別物に変質。テストは通り続けた | 可変キャプチャ付き map は畳み込みの隠れ蓑。リファクタ定石 = **抽出 → 全呼び出し移行 → 旧経路削除 → シード固定テストで数字が小数点まで不変なことを証明** |

### Rust 言語・ツールチェーンの罠

| 事件 | 一般化 |
| --- | --- |
| rustc の `unused_assignments` が入れ子 for 内の dead store を見逃す(vol4、最小再現で確認) | lint を過信しない。dead store の検出はリファクタ定石(数字不変の証明)で補う |
| rustdoc で `[修正版]` が intra-doc link 扱いされ警告(vol1) | doc コメント内の半角 `[...]` は避け、全角【】を使う |
| clippy 更新で vol1 に後方警告 47 件が突然出現 | 本番リポジトリは CI でツールチェーンを固定するか、更新追随の運用を明文化する |

## 3. v1.0 要求リスト

### 既知分(vol1〜vol5 前半までで判明)

- **分布装置を一級市民に**: 正規分布(1D/多変量 — Cholesky サンプリング)、カテゴリカル、
  シード注入 API。GMM/EM で使った部品はそのまま要求仕様。
- **log-space ユーティリティ**: `log_sum_exp`、対数尤度 API(EM・VAE の ELBO・拡散の
  共通土台。f32 では対数空間が「任意」ではなく「必須」)。
- **パリティ儀式のツール化**: golden 生成スクリプトの型(`str(np.float32)` 最短表記、
  npz 橋、シード対応表)を新リポジトリの `tools/` に標準装備。
- **wgpu v2 の継ぎ目**: 数値演算を名前付き関数の裏に隠す規約を最初から。
- **CPU 並列の設計余地**(2026-07-30、UNet 学習 703ms/iter を体感したユーザーの要望):
  rayon によるバッチ軸データ並列・im2col/matmul のスレッド化。GPU(wgpu)が本命だが、
  CPU 経路にも並列の継ぎ目を残す。
- Adam は本家準拠の lr_t 畳み込み実装済み(vol3)— そのまま持ち越し。

### 物理指向の将来要求(2026-07-30 議論 — HNN・微分可能物理)

- **HNN(ハミルトニアン NN)**: 必要部品は「入力勾配を create_graph で保持 → さらに backward」
  = vol3 の二階微分そのもので、**既に充足**。v1.0 の example / 卒業試験後の腕試し候補
  (エネルギー保存は H ドリフトの帯域 assert に翻訳 — 「図→統計量」流儀)。
- **微分可能物理エンジン**: 時間発展ループの backprop は BPTT と同構造(RNN/unchain 実戦済み)。
  追加要求: ①**勾配チェックポインティング**(長軌道のメモリ対策、v1.x 候補)②随伴法
  (Neural ODE 流、stiff 系・陰的積分器用)③不連続(衝突)の勾配は研究最前線 —
  当面は滑らかな系に限定。f32 の長時間積分誤差はシンプレクティック積分器で緩和。
  第一歩候補: 微分可能 CartPole(vol4 の物理を Variable 演算化 — 物理貫通の制御最適化)。
- Oslo 法アイデア A(NN パラメータ化 ρ/T → 物理計算 → χ²)は同族 — 「物理を構造として
  埋め込み勾配を貫通させる」路線として一本化して考える。

### vol5 後半で判明する見込みの要求(写経しながら確定させる)

- VAE(ステップ 7): reparameterization trick、KL の閉形式、エンコーダ/デコーダの
  対称構造、BCE 損失
- 拡散(ステップ 8〜10): ノイズスケジューラ、時刻埋め込み、UNet 部品、条件付け

## 摩擦台帳(vol5 ステップ 6〜10 で随時追記)

写経中に見つかった vol3 の欠品・PyTorch との意味論差・API の使いにくさを記録する。

| 日付 | 発生箇所 | 摩擦 | v1.0 への含意 |
| --- | --- | --- | --- |
| 2026-07-30 | **CPU 並列化の下見(step10 卒業ラン準備での実測)**: ndarray の `matrixmultiply-threading` フィーチャ(コード無改造)を UNet 学習で実測 — batch 16 で 4.90→3.63s、batch 64 で 3.36→2.54 s/iter、**どちらも ~1.32 倍止まり**(10 性能コアで)。行列積以外(im2col/col2im のコピー、BN 縮約、要素演算、逐次 backward)がシングルスレッドのまま残る Amdahl の壁。**決定論は無傷**(スレッド数 1/4/10 で loss 完全一致 — gemm は k 方向を分割しないため加算順序不変)、全巻テスト green。今回は「一晩は一晩」として不採用・revert | matmul スレッド化だけでは CPU 並列の答えにならない(×1.3 が上限)。もうひとつ: UNet::new に label_emb を足したら **RNG 消費列がずれて tier3 の epoch1 loss が 0.2591→0.2750 に移動** — band assert(統計主張)が設計通り吸収し、パリティ側は golden 上書きで不変。「tier3 に正確値を求めない」原則の実証 | v1.0 の CPU 並列は **バッチ/チャネル軸の rayon 並列(im2col・要素演算・縮約を含む全演算子レベル)**で設計する。matmul フィーチャは無料なので入れてよいが主役にはならない。構造体へのフィールド追加が乱数列を割る問題は、**レイヤごとに独立シード(名前から導出等)** で将来解決する価値あり |
| 2026-07-30 | **ステップ 10 条件付き拡散+CFG(golden 生成時の事前登録)**: nn.Embedding は vol3 に無いが新部品は不要 — **one-hot 定数 × Linear(nobias) の合成**が埋め込みの行引きと厳密同値(backward・二階も自動)。おまけに vol3 Linear の W は (in,out) 格納なので PyTorch の embedding weight (10,100) と**転置なしで一致**(他の Linear と逆)。golden のラベルドロップは乱数でなく **drop_flags=[1,1,0,1,1] を手で固定** — 5 iter で条件付き/無条件の両分岐を必ず踏ませ、再現に乱数再生が不要 | **事前登録 1 件**: Adam のステップカウンタは **PyTorch=per-param、DeZero/vol3=global**。差が発火するのは「パラメータが update を欠席した」ときだけで、label dropout の無条件 iter で label_emb が初めて欠席する(勾配 None → 両実装ともスキップ自体は一致 — vol3 Adam が None grad をスキップすることは step09 の None 学習 green で実証済み)。以後の bias correction が PyTorch=step 3 vs vol3=t 4 とズレ、**label_emb/W だけ ~1e-4〜1e-3 の系統ドリフト**が出る予測(二段 tol 1e-2 内)。**実測(同日)**: label_emb/W = 2.47e-3 — tol 内だが、通常重みのドリフト帯(1e-3〜5.4e-3)のただ中に沈み、**ステップカウンタ差の系統成分はカオス床以下で単独分離不能**(step07 の Adam eps 容疑と同じ「機構は実在・検出限界以下」クラス)。二段構造は step09 から再現: 重み max 5.4e-3(down2/conv1/W)/ running_* max 1.76e-2(bot1/bn2/running_mean)、ドリフトはネット中央 bot1 に集中し出力層は静か(out/b 2e-6)— 同じ指紋。iter0 loss は 7 桁一致(1.142302 vs 1.142301)、CFG サンプリング部分軌道 <1e-4 通過 | v1.0 の Adam は **per-param ステップカウンタ**を採用(PyTorch 準拠)。「欠席するパラメータ」は条件付きモデル・マルチタスクで常態化するため、global t は将来のパリティ負債になる。CFG のブレンドは Variable 不要の ndarray 空間で(「モデル呼び出し」と「逆過程の式」を分離した denoise API 設計の配当) |
| 2026-07-30 | **ステップ 9 UNet 学習パリティ(tier1/2)**: iter 0 の loss が 7 桁一致 = Conv/BN(train)/ReLU/MaxPool/Upsample/Concat/pos_encoding/ブロードキャスト加算の意味論を一括証明。5 iter 後のドリフトは**二層構造** — 学習重み max 6e-3(Adam が移動量 ~lr に正規化するため)vs BN running stats max 1.5e-2(0.1×バッチ統計/iter で物差しが別)。ドリフトはネット中央(bot1)に集中し出力層は静か(out/b 1e-6) | **更新機構が違う量は別の tol で測る**(重み 1e-2 / running_* 5e-2 の 2 クラス assert)。サンプリング部分軌道の検証は「golden 重みを再注入してから」— 学習ドリフトを持ち込むと経路の意味論とカオスが混ざって判定不能になる(検証しつつ set_data で注入し直す一石二鳥の設計が有効だった) | パリティ儀式の標準形に「**位相ごとに golden 状態を再注入して隔離**」を追加。損失正規化の流儀(全要素平均 vs バッチ平均)もチェックリスト入り(784 倍罠 — KL 係数事件の同族、今回は事前警告で未遂) |
| 2026-07-30 | **BatchNorm2d(vol3 拡張、UNet 前準備)— 事前登録制の成功例**: 実装前に容疑者 4 件(正規化=biased/蓄積=unbiased の非対称、momentum=0.1 の向き、eps=1e-5 の位置、統計軸=N·H·W)を per-op golden 化 → 初回実装で全 assert 一発通過 | 「**書く前に罠を golden に固定する**」方式は、書いた後にデバッグする方式より速い(今回デバッグ時間ゼロ)。running stats は「params から除外・named_params に含める」— vol3 の 2 系統分離が“学習しない状態”にそのまま適合した | v1.0 の新オペレータ追加手順を「①意味論の容疑者リストアップ → ②per-op golden 生成 → ③実装 → ④全 assert」の順で標準化する |
| 2026-07-29 | **ステップ 7 パリティ儀式の初獲物**: VAE の loss が iter 0 から 0.107 ズレ、学習後は 30.3 vs 38.8 に発散。犯人は KL の係数 — 本の L2 は標準 KL の 2 倍(本は再構成 NLL と KL の両方から ½ を落とした「2×ELBO」流儀で自己整合)、ユーザーは L1=本流儀+KL=標準流儀を混在させ KL の相対重みが半減 | **重み付け・流儀の混在は単体テストで原理的に検出不能**(各項が単体では「正しい」ため)。目的関数の係数はパリティでしか守れない。もう 1 つ: 巨大和(2.5 万要素)の f32 loss に絶対 tol 1e-5 は ε 以下の要求で物理的に不可能 — tol は和の要素数でスケールさせる | パリティ儀式に「**損失関数の係数一致**」を明示項目として入れる。tol 設計は observable の要素数・値域から導出する(一律の magic number にしない)。**修正後の実測**: 10 iter で loss diff 4.6e-5(相対 ≈ ε)・重み max\|Δ\| 5.35e-4(relu 境界のマスク反転による離散分岐と推定)、30 epoch の統計は 38.8194 vs 38.8193(独立乱数で期待値一致 — band assert は緩いまま保つ)。事前登録していた容疑者「Adam の eps 置き場所差(DeZero `√v+eps` vs PyTorch `√v̂+eps`)」は通常の勾配スケールでは検出限界以下と確定・容疑解除 |
| 2026-07-29 | ステップ 7 VAE の複合レイヤ(Encoder は (μ, ln σ²) の 2 出力、VAE の forward は rng が必要)。**2 例目(2026-07-30)**: ステップ 9 UNet — ConvBlock は forward(x, v)、UNet は forward(x, t) でどちらも型に収まらず、同じ unimplemented! 穴埋めを再演 | **Layer trait の抱き合わせ問題**: named_params/save/load が欲しいだけなのに `forward(&Variable) -> Variable` の実装を強制され、多出力・rng 付き・多入力 forward は型に収まらない → `unimplemented!` で穴埋め(= `&dyn Layer` 経由の forward 呼び出しが実行時パニックする地雷) | v1.0 では**パラメータ管理(Module)と関数性(forward)を別 trait に分離**する。PyTorch の nn.Module も forward のシグネチャを型で縛っていない — 縛るなら関連型か、forward を持たない Module trait+個別の呼び出しメソッドで |
| 2026-07-29 | ステップ 6 パリティ予行(sin 回帰 1→10→1、SGD lr=0.2、10⁴ iter) | **摩擦なし** — npz 橋(Python→Rust 方向・W 転置)・sigmoid・MSE・SGD が vol3 無改造で一致。**iter 0 の loss はビット一致**。10⁴ iter 後: loss drift 6.1e-6、重み max \|Δ\| 2.35e-3(**比 ~400 倍** — 重み差は損失曲面の平坦方向に住み、loss は二次でしか感応しない。正しい実装同士が丸めでカオス的に離れた指紋) | **パリティ儀式の三段 tol 設計**: ①iter 0 はビット級(意味論の証明)②序盤 ~100 iter はタイト 1e-5 級(バグ検出力の最大帯 — 丸めはまだ小さく、バグなら即 1e-3 超)③長期は重み一致を要求しない(測れるのはカオスであって正しさではない — 主張は loss 帯域・最終品質など統計量で) |
